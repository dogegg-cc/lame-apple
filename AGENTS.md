# lame-apple 维护约定

- 这是独立的 Apple 平台 LAME 构建工程，修改范围限于本仓库。
- 首版仅支持 iOS arm64 与 iOS Simulator arm64；不增加 x86、macOS、Watch 等平台，除非用户明确要求。
- 官方源码以 `upstream.json` 和 `Vendor` 中的原始发布归档为准。构建前必须核对 SHA-256，不自动追踪 latest。
- 不直接修改解压后的源码；确需上游补丁时，在 `Patches` 保存补丁，更新元数据并说明原因。
- 不引入 FFmpeg、解码器或命令行前端；本库仅用于 PCM 到 MP3 编码。
- 默认生成独立动态 Framework；不要无说明改成静态或可合并库。
- 保留官方源码、许可证和变更依据。动态链接不是 App Store 或许可证合规的自动保证。
- `.build`、`Artifacts`、`Release` 为生成目录，不提交或手工修补；用脚本重建。
- 关键构建约束、安全边界和清理逻辑使用中文注释解释原因。
- 验证至少包括两个 arm64 切片构建、Swift 导入/链接、模拟器 MP3 编码/回读。
- 不得声称已完成未实际执行的真机运行、App Store 验证或远端发布。
- 文档只描述本库，不记录接入方的业务、内部路径或迁移计划。
- 版本、tag 与变更记录遵循 CONTRIBUTING.md；已发布 tag 和资产不得覆盖。
- 只有用户明确要求时，才创建远端仓库、推送或发布 Release。
